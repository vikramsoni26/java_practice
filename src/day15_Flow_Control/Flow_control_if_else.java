package day15_Flow_Control;

public class Flow_control_if_else {
    public static void main(String[] args) {
        /*
        1. What is flow control statement?
           Flow control statement is used to change the flow of execution of program.
        2. What is if?
           If is used to check the condition and when a condition is true then if block will execute
           otherwise if block is not execute.
        3. Syntax of if?
           if (condition){
            //if block
           }
        4. What is if else?
           It is used to check a condition and if block will execute when condition will be true
           otherwise else will execute.
        5. Syntax of if else?
            if (Condition) {
             // if block
            }
            else{
            // else block
            }
        6. What is else if?
           It is used to check multiple condition and when a condition is found true then there
           will be appropriate execution.
        7. Syntax of else if?
           if (Condition) {
             // if block1
            }
            else if (Condition) {
             // if block2
            }
            else{
            // else block
            }
        8. What is Nested if else?
           when if else is used inside another if else then it is called nested if else.
        9. Syntax of Nested if else?
           if (Condition) {
           if (Condition) {
             // code
            }
            else{
            // code
            }
            else{
            if (Condition) {
             // code
             }else{
               // code
             }
             }.
        10. If else based question




         */
    }
}
