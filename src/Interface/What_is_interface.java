package Interface;

public class What_is_interface {
    /*
    1.	What is Interface?
    An Interface in a java is a blueprint of a class that contain abstract method or Constance variable.
    A class implements an interface using the implements keyword.
    Key features
    1. Method are public and abstract by default
    2. variables are public static final by default
    3. Can not create object of an interface
    4. support multiple inheritance
    5. from java 8 interface can have default and static method.
    2. Can we create object of interface?
    No we can not create object of interface.
    3. Why interface does not allow object creation?
    The interface does not allow abject creation because it follow multiple inheritance.
    4. Default methods in interface (Java 8)
•	Yes The method in interface are default.



     */
}
